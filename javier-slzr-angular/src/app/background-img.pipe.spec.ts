import { BackgroundImgPipe } from './background-img.pipe';

describe('BackgroundImgPipe', () => {
  it('create an instance', () => {
    const pipe = new BackgroundImgPipe();
    expect(pipe).toBeTruthy();
  });
});
